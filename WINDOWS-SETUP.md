# Windowsデスクトップで Bカウンターを編集できるようにする手順（初心者向け）

このとおりに進めれば、MacBookと同じように **Windowsから Bカウンターのアプリを編集・反映** できるようになります。
専門知識は不要です。上から順番にやってください。

---

## 0. 用意するもの

- Windowsのデスクトップパソコン（ネットにつながっていること）
- **GitHubのアカウント**（ユーザー名：`gymkoki`）のログイン情報（メール／パスワード）
- **Claudeのアカウント**（普段Macで使っているもの）のログイン情報

> ※ 何かをダウンロードしたり、ログイン画面が出たりしますが、すべて正規の手順です。安心して進めてください。

---

## 1. 【かんたん版】PowerShellに1行はりつけるだけ

### ① PowerShellを「管理者として」開く

1. 画面左下の**スタート**（Windowsマーク）をクリック
2. キーボードで `powershell` と入力
3. 出てきた「**Windows PowerShell**」を**右クリック** →「**管理者として実行**」をクリック
4. 「このアプリがデバイスに変更を加えることを許可しますか？」と出たら「**はい**」

### ② 下のコマンドを1行コピーして、PowerShellに貼り付けてEnter

> 貼り付けは、PowerShellの黒い画面で**右クリック**するとできます。

```powershell
irm https://raw.githubusercontent.com/gymkoki/ab-diet-counter/main/setup-windows.ps1 | iex
```

これで自動的に、必要なもの（Git・Node.js・Claude Code）が順番にインストールされ、
デスクトップに **`ab-diet-app`** というフォルダができて、最新のコードがダウンロードされます。
（パソコンやネットの速さによっては5〜15分ほどかかります。途中で止まったように見えても待ってください）

### ③ 最後に「今すぐ Claude Code を起動しますか？」と聞かれたら `y` を入力してEnter

初めて起動したときは、**Claudeへのログイン画面（ブラウザ）**が出ます。
普段Macで使っているClaudeアカウントでログインしてください。

これで準備完了です🎉

---

## 2. 【毎日の使い方】次回からはこれだけ

1. **PowerShell** を開く（管理者でなくてOK）
2. 下の2行を1行ずつ貼り付けてEnter：

```powershell
cd $env:USERPROFILE\Desktop\ab-diet-app
claude
```

3. Claude Codeが起動したら、Macのときと同じように**日本語で話しかける**だけです。
   例：「履歴の文字を大きくして」「○○のボタンを追加して」など。

### 変更を本番に反映するとき

「**反映して**」と伝えれば、私（Claude）が GitHub に保存し、Renderが自動で本番サイトに反映します。

- 初めて反映するときだけ、**GitHubのログイン画面（ブラウザ）**が出ることがあります。
  → ユーザー名 `gymkoki` でログイン → 「Authorize（許可）」を押してください。
  一度ログインすれば、次回からは自動です。

---

## 3. 困ったときは

**Q. 「irm ...」のコマンドが赤い文字でエラーになる**
→ PowerShellを**「管理者として実行」**で開き直してから、もう一度貼り付けてください。

**Q. 「winget が見つかりません」と出た（古いWindows）**
→ 下の2つを手動でインストールしてから、もう一度①のコマンドを実行してください（クリックで進めるだけ）：
- Git: https://git-scm.com/download/win
- Node.js（LTS版）: https://nodejs.org

**Q. GitHubのパスワードを忘れた／ログインできない**
→ https://github.com/login で「Forgot password?」から再設定できます。

**Q. Claude にログインできない**
→ 普段Macで使っているのと同じアカウント（メール）でログインしてください。

---

## 4. 補足（知っておくと安心）

- **アプリのコードはすべて GitHub に保存**されています。MacBookが壊れても、このWindowsから続きの作業ができます。
- **動いているサービス（本番サイト）は Render** にあり、どのパソコンが壊れても止まりません。
- **利用者のデータ（Bカウント・体重・写真）は Render のデータベース**にあり、こちらも安全です。
- セキュリティのため、Mac側のGit設定に古いトークンが残っている場合は再発行をおすすめします。
  Windowsでは上記の**ブラウザログイン方式**を使うので、トークンの埋め込みは不要です。

---

## （おまけ）iPhoneから緊急で文字だけ直したいとき

1. iPhoneのブラウザで https://github.com/gymkoki/ab-diet-counter を開いてログイン
2. 直したいファイル（例：`templates/index.html`）を開く → 鉛筆マークで編集 → 「Commit changes」
3. Renderが自動で反映します。
   ※ ただし本格的な編集はWindowsのClaude Codeで行うのがおすすめです。
