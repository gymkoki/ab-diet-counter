services:
  - type: web
    name: ab-diet-counter
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
